import { StyleSheet } from "react-native";

export default StyleSheet.create({
  container: {
    flex: 1,
    padding: 15,
  },

  row: {
    justifyContent: "space-between",
    marginBottom: 16,
  },
});
